python -m venv myEnv
pip install django

pip install python-dotenv

pip install djangorestframework
pip install django-cors-headers

pip install psycopg2        # allowing Python code to connect, interact with PostgreSQL database.

pip install PyJWT